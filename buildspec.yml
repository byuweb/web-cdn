version: 0.2

phases:
  install:
    commands:
      - . .codebuild/setup.sh
      - lerna bootstrap
      - lerna exec -- chmod a+x ./codebuild.sh
  build:
    commands:
      - lerna ls
      - lerna exec -- ./codebuild.sh

